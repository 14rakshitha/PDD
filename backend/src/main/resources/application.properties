spring.application.name=lawvoice-backend
server.port=${PORT:8080}

app.cors.allowed-origin=${FRONTEND_ORIGIN:http://localhost:5190}

# Database Configuration (Supabase PostgreSQL via Pooler - Seoul aws-1)
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres?sslmode=require}
spring.datasource.username=${JDBC_DATABASE_USERNAME:postgres.bqmwqxyjzipuitrqkgff}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:rakshi14@baskaran}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate properties
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
